// Used in the following components: SignIn.jsx and SignUp.jsx

// This function is used to toggle the password visibility in the corresponding input fields
export const togglePassword = (passwordShown, setPasswordShown) => {
  setPasswordShown(!passwordShown);
};
