import ClipLoader from "react-spinners/ClipLoader";

// This component is used to display a loading spinner while the app is fetching data from the server

const Loading = () => {
  return <ClipLoader color="#36d7b7" />;
};

export default Loading;
